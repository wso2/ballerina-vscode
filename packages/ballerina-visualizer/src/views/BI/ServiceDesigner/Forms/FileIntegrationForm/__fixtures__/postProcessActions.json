{
    "_comment": "Edit mode with an 'After File Processing' post-process action (moveTo) carrying choices and a nested TEXT+EXPRESSION property. Drives renderPostProcessActionSection (checkbox + radios) and renders the child TextExpressionField, exercising the RPC mock. The required nested 'targetPath' is empty, so Save is disabled until a value is entered (covers the validation-gate branch).",
    "name": "post-process-actions",
    "isNew": false,
    "selectedHandler": "onCreate",
    "filePath": "/project/service.bal",
    "model": {
        "id": 0,
        "name": "ftpService",
        "type": "ftp",
        "moduleName": "ftp",
        "orgName": "ballerinax",
        "version": "1.0.0",
        "packageName": "ftp",
        "listenerProtocol": "ftp",
        "icon": "",
        "functions": []
    },
    "functionModel": {
        "metadata": { "label": "onCreate", "description": "Triggered when a file is created" },
        "kind": "REMOTE",
        "enabled": true,
        "optional": false,
        "editable": true,
        "codedata": { "lineRange": { "fileName": "service.bal", "startLine": { "line": 5, "offset": 4 }, "endLine": { "line": 12, "offset": 5 } } },
        "name": { "metadata": { "label": "JSON", "description": "Handle the file as JSON" }, "value": "onCreate", "enabled": true, "editable": false },
        "properties": {
            "annotations": {
                "metadata": { "label": "Annotations", "description": "" },
                "properties": {
                    "postProcessAction": {
                        "metadata": { "label": "After File Processing", "description": "What to do with the file after it is processed" },
                        "properties": {
                            "moveTo": {
                                "metadata": { "label": "Move file after processing", "description": "Move the file to another location after processing" },
                                "enabled": true,
                                "choices": [
                                    {
                                        "value": "MOVE",
                                        "enabled": true,
                                        "metadata": { "label": "Move", "description": "Move the file" },
                                        "properties": {
                                            "targetPath": {
                                                "metadata": { "label": "Target path", "description": "Destination path for the moved file" },
                                                "optional": false,
                                                "value": "",
                                                "types": [{ "fieldType": "TEXT" }, { "fieldType": "EXPRESSION" }]
                                            }
                                        }
                                    },
                                    {
                                        "value": "DELETE",
                                        "enabled": false,
                                        "metadata": { "label": "Delete", "description": "Delete the file" },
                                        "properties": {}
                                    }
                                ]
                            }
                        }
                    }
                }
            }
        },
        "parameters": [],
        "returnType": { "metadata": { "label": "Return Type", "description": "" }, "value": "error?", "enabled": true, "editable": false }
    }
}
