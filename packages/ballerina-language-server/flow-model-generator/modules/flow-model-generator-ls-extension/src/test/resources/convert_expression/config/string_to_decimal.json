{
  "description": "Convert string to decimal using decimal:fromString with error handling",
  "output": "output.decimalField",
  "outputType": "decimal",
  "expression": "input.stringField",
  "expressionType": "string",
  "convertedExpression": "decimal:fromString(input.stringField) is error ? 0.0d : check decimal:fromString(input.stringField)"
}
