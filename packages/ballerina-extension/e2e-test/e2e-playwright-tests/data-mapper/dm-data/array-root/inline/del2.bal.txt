import ballerina/log;

public function main() returns error? {
    do {
        InRoot[] input = [];
        OutRoot[] output = from var inputItem in input
            select {
                oArr1D: from var iArr1DItem in inputItem.iArr1D
                    select {}
            };
    } on fail error e {
        log:printError("Error occurred", 'error = e);
        return e;
    }
}
